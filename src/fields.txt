
num                     meta x
slug                    meta x
title                   meta x
difficulty              info x

constraints             info |
description             info |-  parse html content
examples                info |

signature               info |
params                  info |- parse signature in code snippet
return type             info |

synced code             code





IDEA:
>>> import inspect

>>> func_str = 'def add(a: int, b):'

>>> exec(func_str + 'pass')
>>> func_name = re.search(r'def (\w+)\(', func_str).group(1)
>>> func = eval(func_name)
>>> params = inspect.signature(func).parameters
>>> for name, param in params.items():
...     print(param)
...
a: int
b